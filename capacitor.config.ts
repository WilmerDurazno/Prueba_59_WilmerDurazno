import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Prueba59_WilmerDurazno',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
